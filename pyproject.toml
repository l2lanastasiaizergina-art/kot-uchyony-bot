[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "kot-uchyony-bot"
version = "0.5.0"
description = "«Кот Учёный» — Telegram Mini App с орфографией и Библиотекой лидера"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Kot Uchyony contributors"}]
dependencies = [
  "aiogram>=3.31,<4",
  "aiohttp>=3.11,<4",
  "python-dotenv>=1.1,<2",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<9",
  "pytest-asyncio>=0.25,<1",
  "ruff>=0.11,<1",
]

[project.scripts]
orthogame = "ortho_game_bot.app:run"
orthogame-init-db = "ortho_game_bot.cli:init_db"
orthogame-import-content = "ortho_game_bot.cli:import_content"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ortho_game_bot.database.migrations" = ["*.sql"]
"ortho_game_bot.webapp" = ["static/*", "static/assets/*", "static/assets/word_packs/*", "static/assets/art_culture_packs/*"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
